# Build Journal

- 2026-07-09: Started. Env: node v24.17.0, npm 11.17.0, git 2.43.0, gh 2.92.0. Dir empty except SPEC.md. Not a git repo yet.

- 2026-07-09: Scaffolded SvelteKit (kit 2.63/vite8/svelte5.56/TS6). New `sv` puts adapter inline in vite.config — moved kit config to canonical svelte.config.js and made vite `sveltekit()` plain; works. TTY prompts blocked automating tailwind add-on & vitest usages; added Tailwind v4 manually via @tailwindcss/vite. ultracite init --quiet wired oxlint+oxfmt configs; it overwrote `check` script — restored to svelte-check, set lint=oxlint/format=oxfmt.
- 2026-07-09: List page renders (SPA, ssr=false + prerender shells). `{#each Array.from({length:n}), i (i)}` index-only each syntax works in Svelte 5. Master index via one /pokemon?limit=100000 call; details fetched per-slice with in-memory cache. Screenshot verified: cards, sprites, type badges, hearts, gradients all good.
- 2026-07-09: Decision: pre-push hook runs unit tests only (not e2e) to avoid slow/browser-dependent git pushes; CI runs full e2e. base path=/pokedex-tw-opuslo-off-1.

- 2026-07-09: ultracite oxlint `sveltekit` preset subpath doesn't exist (it's `svelte`). Fixed import. Preset extremely strict — disabled purely-stylistic rules (filename-case vs PascalCase components, sort-keys, func-style, prefer-await-to-then in $effects, require-unicode-regexp, etc). All correctness rules kept. Documented in DECISIONS.md.
- 2026-07-09: Build prerender crawler errored: dynamic [name] routes inherited prerender=true from layout but weren't crawlable. Fixed with `export const prerender = false` on both dynamic +page.ts. Added static/.nojekyll for Pages (_app dir).
- 2026-07-09: e2e flakiness — 4 tests (detail/404/favorites/berries) timed out under parallel load. Root cause: detail load() awaited pokemon+species+evolution SEQUENTIALLY before render, exceeding 15s under PokeAPI load. Verified pages actually render fine when loaded standalone (preview 4173, direct nav returns 200 + hydrates). Fix: detail load only awaits core pokemon; species/evolution/flavor moved to a client $effect → instant paint + reliable tests. Also playwright: workers=2, retries=1, expect timeout 15s, baseURL includes /pokedex-tw-opuslo-off-1.
- 2026-07-09: Unit component test hit Svelte server build under vitest — fixed with resolve.conditions:['browser'] on client test project. 19 unit tests pass.

- 2026-07-09: Lighthouse (chrome-devtools lighthouse_audit, desktop): A11y 100, Best-Practices 100, SEO 100 (after fixing heading-order h1→h2 on cards). lighthouse_audit excludes Performance, so ran standalone `lighthouse@12` CLI w/ playwright chromium.
- 2026-07-09: Perf was 86 then dropped to ~80 — root cause: LCP element was a ~195KB official-artwork PNG; 30 of them (~4.5MB) saturate Lighthouse's throttled desktop link. First bad standalone run reported CLS 0.602 but that was a broken Chrome connection (real CLS 0.02–0.03). Fix: grid cards now use pixel `front_default` sprites (~0.6KB each, 200× smaller) rendered image-rendering:pixelated; official artwork kept for detail hero. Perf 86/80 → **98** (LCP 1.1s, CLS 0.024, TBT 0, FCP 0.4s). Also added preconnect to pokeapi.co + raw.githubusercontent.com and fetchpriority=high on eager card images.
- 2026-07-09: CI green (verify+deploy), live URL https://azagatti.github.io/pokedex-tw-opuslo-off-1/ loads + deep-links (/pokemon/pikachu/) work via 404.html fallback. Repo didn't pre-exist despite SPEC — created it via gh. Enabled Pages (build_type=workflow) via gh api.
